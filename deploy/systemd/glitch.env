# Glitch Server Environment — systemd deployment
# Copy to /etc/glitch/glitch.env and adjust values.

# Admin panel password (required)
GLITCH_ADMIN_PASSWORD=changeme

# PostgreSQL connection URL (required for data persistence)
GLITCH_DB_URL=postgres://glitch:glitch@localhost:5432/glitch?sslmode=disable
